/**
 * Act 4.5a — THE DOORWAY, first measure.
 *
 * A small child in the door, arms going. The valley is farmed, the range is
 * clean behind it, and there is no wire over the road. See `./doorway.tsx`
 * for the montage this is the first four seconds of.
 */
import { makeDoorwayScene } from './doorway'

export default makeDoorwayScene(0)
