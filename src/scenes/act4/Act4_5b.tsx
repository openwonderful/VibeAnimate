/**
 * Act 4.5b — THE DOORWAY, second measure.
 *
 * Halfway up the post. The fields are going — bays out of production, dry
 * earth spreading — the power line has arrived, and the first towers are up
 * behind the range. See `./doorway.tsx`.
 */
import { makeDoorwayScene } from './doorway'

export default makeDoorwayScene(1)
