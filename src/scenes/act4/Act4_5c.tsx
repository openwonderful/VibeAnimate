/**
 * Act 4.5c — THE DOORWAY, third measure.
 *
 * Nearly grown, and the third line goes on the post above the other two. No
 * fields at all now — just worked-over dirt — and the city fully risen behind
 * the range. See `./doorway.tsx`.
 */
import { makeDoorwayScene } from './doorway'

export default makeDoorwayScene(2)
