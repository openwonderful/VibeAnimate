/**
 * In the Air — `?act=8b-air-act1`. Nothing but R7, the 청사초롱 from 1.1-B.
 *
 * The one design in the set with hardware, and the only one whose silhouette
 * carries anything below the envelope. A whole sky of tassels is a different
 * proposition from a few scattered through a mix, which is the thing this is
 * for.
 */
import { AirScene } from './LanternAir'

export default function LanternAirAct1() {
  return <AirScene set="R7" />
}
