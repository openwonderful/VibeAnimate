/**
 * In the Air — `?act=8b-air-drum`. Nothing but R6 drums.
 *
 * The control for the mix. A mixed field always looks livelier than any of its
 * members, which makes it a flattering way to judge one — this asks whether
 * the drum can carry a sky on its own.
 */
import { AirScene } from './LanternAir'

export default function LanternAirDrum() {
  return <AirScene set="R6" />
}
