/** 3.3 — "THE ROAD HOME" (1:16 → 1:22). CUT. The last stretch: the child up
 *  on the parent's back, the camera arcing out to three-quarters with the
 *  house past their shoulders, and the parent's light going as the child's
 *  comes up past it. */
import { makeFlightScene } from './scene'
export default makeFlightScene(52, 'Act3_3')
