/** 1.3 — "THE STADIUM" (0:16 → 0:20.4). Out of the city, across the apron,
 *  and up over the wall on "I need the whole stadium to jump". */
import { makeFlightScene } from './scene'
export default makeFlightScene(16, 'Act1_3')
