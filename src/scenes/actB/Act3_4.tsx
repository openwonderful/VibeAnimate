/** 3.4 — "THE TABLE" (1:22 → 1:36). CUT. Inside, after the walk home: the
 *  hanok at 20×, the two of them across a soban, one slow push in. */
import { makeFlightScene } from './scene'
export default makeFlightScene(58, 'Act3_4')
