/** ACT 1 — "THE APPROACH" (0:00 → 0:26). The whole act in one window: the
 *  pass, the star punch on "I need", the city canyon, the stadium, over the
 *  rim, and down onto the stage as the seven start landing. */
import { makeFlightScene } from './scene'
export default makeFlightScene(0, 'Act1')
