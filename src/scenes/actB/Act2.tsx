/** ACT 2 — "THE SHOW" (0:26 → 0:45). The one moment in the opening that is
 *  not about travelling, and then the longest single move in the film: the
 *  seven arrive on the stage, and the camera leaves mid-routine and runs all
 *  the way back out to the pass, arriving as the chorus lands at 0:44.99. */
import { makeFlightScene } from './scene'
export default makeFlightScene(26, 'Act2')
