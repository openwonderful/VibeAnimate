/** 1.2-B — "THE CITY + THE WALL", alternate cut (0:08 → 0:22). The window
 *  that holds the whole difference: all four "I need" signs dead centre, the
 *  early shallow nudges under them, and the high decelerating arrival at the
 *  stadium on 0:18.65. This is the one to watch against `1.2`+`1.3`. */
import { makeFlightScene } from './scene'
import { flightPoseB, SIGN_X_B } from './flightApproachB'
export default makeFlightScene(8, 'Act1_2B', flightPoseB, SIGN_X_B)
