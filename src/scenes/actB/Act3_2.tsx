/** 3.2 — "THE TREE" (1:04 → 1:16). CUT. Night, on the verge, the two of them
 *  against the trunk. Stationary, then a zoom as their heads come up, then a
 *  crane until the sky is what is in front of the lens. */
import { makeFlightScene } from './scene'
export default makeFlightScene(40.5, 'Act3_2')
