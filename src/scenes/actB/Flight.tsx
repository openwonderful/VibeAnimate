/**
 * Acts 1–3, complete: 0:00 → 1:36 in one world and (almost) one camera.
 *
 * The range, the punch through the stars on "I need", the city canyon, the
 * stadium on the plain, over the rim into the bowl, the seven arriving — and
 * then the whole thing in reverse, out past the pass, down onto the road at
 * golden hour, under the tree at night, up to the door, and in to the table.
 * Every other key in Acts 1, 2 and 3 is a WINDOW into this take; play them
 * back to back and the result is frame-identical to playing this.
 *
 * This is the one to watch when judging whether the opening holds together
 * as a single move.
 */
import { makeFlightScene } from './scene'
export default makeFlightScene(0, 'Flight')
