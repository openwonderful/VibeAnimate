/** 2.1 — "THE SEVEN" (0:26 → 0:31). The shot settles on the stage and the
 *  seven arrive, in Act 2.2's order, colours and stagger. The dance never
 *  resolves: at 0:31 the camera starts backing away. */
import { makeFlightScene } from './scene'
export default makeFlightScene(26, 'Act2_1')
